<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    tools:context=".emulator.EmulatorActivity">

    <!--
        The SurfaceView that the native Vulkan renderer draws into.
        It fills the entire screen; the native layer clips/letterboxes as needed.
    -->
    <com.xenia.android.emulator.WindowSurfaceView
        android:id="@+id/emulator_surface_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</FrameLayout>
